[project]
name = "personal-website"
version = "0.1.0"
description = "Offline analytics scripts and external API clients for the site"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "requests>=2.34.2",
    "python-dotenv>=1.0.0",
]
